{
  "extends": "expo/tsconfig.base",  // ✅ Estende config do Expo (correto)
  "compilerOptions": {
    "strict": true,                 // ✅ Tipagem rigorosa (bom!)
    "baseUrl": ".",                 // ✅ Caminho base (correto)
    "skipLibCheck": true,           // ✅ Ignora erros de tipos (bom!)
    "paths": {
      "@/*": ["./*"]                // ✅ Alias de imports (correto)
    }
  }
}